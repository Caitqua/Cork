import tkinter as tk
from tkinter import ttk
from PIL import Image, ImageTk
from datetime import *

class Dashboard:
    def __init__( self, window ):
        self.window = window
        self.window.title('Cork Dashboard')
        self.window.geometry
    f_cases = ttk.Frame()
